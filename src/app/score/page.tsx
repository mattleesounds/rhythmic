import React from "react";
import Score from "../components/Score";

const page = () => {
  return (
    <main className="bg-slate-200 h-screen">
      <Score />
    </main>
  );
};

export default page;
